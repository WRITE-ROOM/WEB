import React from 'react'
import * as S from './LoginButton.style'

export default function LoginButton() {
  return (
    <div>
			<S.LoginButton>
				<button
				onClick={() => {
				}}>로그인</button>
			</S.LoginButton>
		</div>
	)
}
